//
//  Filters.swift
//  WindRose
//
//  Created by Mathew Gacy on 10/23/24.
//  Copyright © 2024 Mobelux. All rights reserved.
//

import Foundation

/// Utilities for applying backdrop blur filters to an element.
///
/// https://tailwindcss.com/docs/backdrop-blur
public enum BackdropBlur: NamespaceProvider {
    public static let namespace = "backdrop-blur"
}

/// Utilities for applying backdrop brightness filters to an element.
///
/// https://tailwindcss.com/docs/backdrop-brightness
public enum BackdropBrightness: NamespaceProvider {
    public static let namespace = "backdrop-brightness"
}

/// Utilities for applying backdrop contrast filters to an element.
///
/// https://tailwindcss.com/docs/backdrop-contrast
public enum BackdropContrast: NamespaceProvider {
    public static let namespace = "backdrop-contrast"
}

/// Utilities for applying backdrop grayscale filters to an element.
///
/// https://tailwindcss.com/docs/backdrop-grayscale
public enum BackdropGrayscale: NamespaceProvider {
    public static let namespace = "backdrop-grayscale"
}

/// Utilities for applying backdrop hue-rotate filters to an element.
///
/// https://tailwindcss.com/docs/backdrop-hue-rotate
public enum BackdropHueRotate: NamespaceProvider {
    public static let namespace = "backdrop-hue-rotate"
}

/// Utilities for applying backdrop invert filters to an element.
///
/// https://tailwindcss.com/docs/backdrop-invert
public enum BackdropInvert: NamespaceProvider {
    public static let namespace = "backdrop-invert"
}

/// Utilities for applying backdrop opacity filters to an element.
///
/// https://tailwindcss.com/docs/backdrop-opacity
public enum BackdropOpacity: NamespaceProvider {
    public static let namespace = "backdrop-opacity"
}

/// Utilities for applying backdrop saturation filters to an element.
///
/// https://tailwindcss.com/docs/backdrop-saturate
public enum BackdropSaturate: NamespaceProvider {
    public static let namespace = "backdrop-saturate"
}

/// Utilities for applying backdrop sepia filters to an element.
///
/// https://tailwindcss.com/docs/backdrop-sepia
public enum BackdropSepia: NamespaceProvider {
    public static let namespace = "backdrop-sepia"
}

/// Utilities for applying blur filters to an element.
///
/// https://tailwindcss.com/docs/blur
public enum Blur: NamespaceProvider {
    public static let namespace = "blur"
}

/// Utilities for applying brightness filters to an element.
///
/// https://tailwindcss.com/docs/brightness
public enum Brightness: NamespaceProvider {
    public static let namespace = "brightness"
}

/// Utilities for applying contrast filters to an element.
///
/// https://tailwindcss.com/docs/contrast
public enum Contrast: NamespaceProvider {
    public static let namespace = "contrast"
}

/// Utilities for applying drop-shadow filters to an element.
///
/// https://tailwindcss.com/docs/drop-shadow
public enum DropShadow: NamespaceProvider {
    public static let namespace = "drop-shadow"
}

/// Utilities for applying grayscale filters to an element.
///
/// https://tailwindcss.com/docs/grayscale
public enum Grayscale: NamespaceProvider {
    public static let namespace = "grayscale"
}

/// Utilities for applying hue-rotate filters to an element.
///
/// https://tailwindcss.com/docs/hue-rotate
public enum HueRotate: NamespaceProvider {
    public static let namespace = "hue-rotate"
}

/// Utilities for applying invert filters to an element.
///
/// https://tailwindcss.com/docs/invert
public enum Invert: NamespaceProvider {
    public static let namespace = "invert"
}

/// Utilities for applying saturation filters to an element.
///
/// https://tailwindcss.com/docs/saturate
public enum Saturate: NamespaceProvider {
    public static let namespace = "saturate"
}

/// Utilities for applying sepia filters to an element.
///
/// https://tailwindcss.com/docs/sepia
public enum Sepia: NamespaceProvider {
    public static let namespace = "sepia"
}
